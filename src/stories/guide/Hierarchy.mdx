import { Canvas, Meta } from "@storybook/blocks";
import { CardTemplate } from "../../components/layout/Template";
import { color } from "../../styles/color";
import "../globalStyles.css";
import { InvertCardTemplate } from "./Hierarchy.stories";

<Meta title="Design System/Guide/Hierarchy" />

# 계층(Hierarchy)

<table style={{ width: "100%", tableLayout: "fixed", margin: "40px 0" }}>
  <thead>
    <tr style={{ backgroundColor: color.surface.tertiary, color: color.text.secondary }}>
      <th>Area</th>
      <th>Desktop</th>
      <th>Mobile</th>
    </tr>
  </thead>
  <tbody>
    <tr style={{ backgroundColor: color.surface.primary }}>
      <td>Heading(H1) &lt;-&gt; Body</td>
      <td>80px</td>
      <td>64px</td>
    </tr>
    <tr style={{ backgroundColor: color.surface.primary }}>
      <td>Title(H1~3) &lt;-&gt; Body</td>
      <td>40px</td>
      <td>40px</td>
    </tr>
    <tr style={{ backgroundColor: color.surface.primary }}>
      <td>Title(H4~6) &lt;-&gt; Body</td>
      <td>32px</td>
      <td>32px</td>
    </tr>
    <tr style={{ backgroundColor: color.surface.primary }}>
      <td>Body &lt;-&gt; Body</td>
      <td>24px</td>
      <td>24px</td>
    </tr>
  </tbody>
</table>

<Canvas of={InvertCardTemplate} sourceState="none">
  <p>주요 콘텐츠의 보조 Detail M / jung456@gmail.com</p>
</Canvas>

<p>
  Heading은 페이지 및 템플릿의 역할 및 기능을 나타내는 컴포넌트, 텍스트와 관련 엘리먼트가 결합한
  컴포넌트로 정보 계층 구조에 따라 사용을 유의합니다.
</p>
<p>
  헤드라인은 작은 화면의 짧고 강조가 높은 텍스트에 가장 적합합니다. 이러한 스타일은 텍스트의 주요
  구절이나 콘텐츠의 중요한 영역을 표시하는 데 좋습니다.
</p>
